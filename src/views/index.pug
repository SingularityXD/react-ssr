<!DOCTYPE html>
html
    head
        meta(charset="UTF-8")
        helmetData.title.toString( )
        helmetData.meta.toString( )
        title React SSR
    body
        #app #{reactDom}
        script window.REDUX_DATA =#{JSON.stringify( reduxState )}
        script(src="./app.bundle.js")